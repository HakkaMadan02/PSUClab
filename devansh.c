#include<stdio.h>

int main()
{
int x;
printf("\n enter a number: ");
scanf("%d", &x);

if (x>10)
{
 printf("\n %d is greater than 10", x);
}

else
{
printf("\n %d is small", x);
}
return 0;
}
